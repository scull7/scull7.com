+++
id = "T-1"
title = "Refresh resume.json career facts and existing views"
status = "done"
rank = "i"
points = 3
labels = ["content", "phase1"]
start_at = "2026-08-14T15:39:40.628172Z"
done_at = "2026-08-14T15:48:23.450176Z"
commits = ["bafc50c"]
created = "2026-08-14T06:25:35.656539Z"
updated = "2026-08-14T15:48:23.450176Z"
+++

## why
Visitors and scrapers should see the authorized 2026 career story from JSON Resume, not stale July copy.

## scope_in
- Update `public/resume.json` only: `basics.summary` (draft A facts), one new TensorWave **work** highlight for the authorized Series B / AMD-cloud context, `projects` entries for **crossr-skills** then **ferro-wg**, top-level `philosophy` string (authorized copy; wording may tighten, facts may not), bump `meta.lastModified` to the actual edit timestamp.
- Keep existing TensorWave IC / leadership / Event API bullets, cents, ReasonML set, and TensorWave Relay project.
- Tag new OSS projects so existing home / experience / highlights / `opensource.md` mapping shows them from JSON (JSON remains SoT). Existing views may get the smallest filter tweak if a keyword is required; no new chrome.
- One-line pointer at the top of `docs/ROADMAP.md` that this slice is governed by `docs/plans/phase1-content-branding-avril-intent.md` and `docs/plans/phase1-content-branding-avril-blessed-backlog.md`. Do not rewrite the rest of ROADMAP.

## scope_out
- New buffers, `:mail`, last-updated UI, JSON-LD, OG/PNG, profile/DNS runbooks.
- Valuation or Series B numbers in `basics.summary`.
- Christian / Father in summary or philosophy.
- 10k GPUs, data-center names, customer names, unpublished Relay internals, “I raised this.”
- velumobex. Education rewrite. `basics.label` change. PDF / print / `:print`.
- Splitting `Main.elm`. New third-party dependencies.

## acceptance_criteria
- [x] `basics.summary` states: Director of Engineering at TensorWave; primary IC on Relay, the control plane behind the largest AMD-based GPU clouds; ~20 years as a builder-leader across AI infrastructure, financial services, data platforms, manufacturing, and SaaS; scales orgs (100+ engineers over a career) and still ships hard systems (Rust, OCaml, ReasonML, Haskell, JavaScript); Founder/CTO of three ventures.
- [x] Summary does **not** contain $350M, $1.55B, Christian, Father, GPU counts, site names, or “I raised this.”
- [x] TensorWave `work[0].highlights` still includes the existing IC / leadership / Event API lines **and** a new highlight that TensorWave raised **$350M Series B** at **$1.55B** post-money and is an AMD-powered / AMD-exclusive AI cloud (employer context).
- [x] `projects[0]` is `crossr-skills` with url `https://scull7.github.io/crossr-skills/` and description equivalent to: Agent skills + harness for long-running coding agents (“forged in the Cross”). Secondary GitHub `https://github.com/scull7/crossr-skills` is stored on the project object (not hard-coded in a view).
- [x] `projects[1]` is `ferro-wg` with url `https://github.com/scull7/ferro-wg` and description equivalent to: Rust WireGuard client with TUI and swappable backends (boringtun, neptun, gotatun).
- [x] cents, bs-result, bs-sql-composer, bs-sql-common, bs-mysql2, and TensorWave Relay remain; no velumobex project.
- [x] Home buffer shows the new summary paragraphs from JSON. `experience.md` / `highlights.md` show the new TensorWave highlight from JSON. `opensource.md` lists crossr-skills then ferro-wg with those one-liners and primary URLs, then the existing OSS set.
- [x] Top-level `philosophy` is the authorized two-paragraph text (MES → HSM → GPU-cloud control planes; isolate effects / keep the core pure / measure the line; AVRIL plans, AXEL executes, nothing ships unblessed; crossr-skills is that discipline made public). It is **not** concatenated into `basics.summary`.
- [x] `meta.lastModified` is a real ISO-8601 timestamp of this edit and is not `2026-07-12T00:00:00`.
- [x] Education / certificates / `basics.label` are unchanged.
- [x] `docs/ROADMAP.md` first content paragraph (or callout) points at the two `docs/plans/phase1-content-branding-avril-*.md` files and does not rewrite Phases 0.2–5.

## dependencies
none

## notes

## Execution Evidence
- GAN: Reviewer BLESS · Tester BLESS · Architect BLESS
- Commit: bafc50c
- `npm run elm:make` Success (8 modules)
- Tester evidenced AC 1–11 (JSON facts, mapping via Resume.elm/Views.elm, ROADMAP pointer-only diff)
- Non-goals honored: no new buffers, no :mail, no last-updated UI, no JSON-LD/OG
