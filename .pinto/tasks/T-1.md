+++
id = "T-1"
title = "Refresh resume.json career facts and existing views"
status = "todo"
rank = "i"
points = 3
labels = ["content", "phase1"]
created = "2026-08-14T06:25:35.656539Z"
updated = "2026-08-14T06:25:35.656539Z"
+++

## why
Visitors and scrapers should see the authorized 2026 career story from JSON Resume, not stale July copy.

## scope_in
- Update `public/resume.json` only: `basics.summary` (draft A facts), one new TensorWave **work** highlight for the authorized Series B / AMD-cloud context, `projects` entries for **crossr-skills** then **ferro-wg**, top-level `philosophy` string (authorized copy; wording may tighten, facts may not), bump `meta.lastModified` to the actual edit timestamp.
- Keep existing TensorWave IC / leadership / Event API bullets, cents, ReasonML set, and TensorWave Relay project.
- Tag new OSS projects so existing home / experience / highlights / `opensource.md` mapping shows them from JSON (JSON remains SoT). Existing views may get the smallest filter tweak if a keyword is required; no new chrome.
- One-line pointer at the top of `docs/ROADMAP.md` that this slice is governed by `docs/plans/phase1-content-branding-avril-intent.md` and `docs/plans/phase1-content-branding-avril-blessed-backlog.md`. Do not rewrite the rest of ROADMAP.

## scope_out
- New buffers, `:mail`, last-updated UI, JSON-LD, OG/PNG, profile/DNS runbooks.
- Valuation or Series B numbers in `basics.summary`.
- Christian / Father in summary or philosophy.
- 10k GPUs, data-center names, customer names, unpublished Relay internals, “I raised this.”
- velumobex. Education rewrite. `basics.label` change. PDF / print / `:print`.
- Splitting `Main.elm`. New third-party dependencies.

## acceptance_criteria
- [ ] `basics.summary` states: Director of Engineering at TensorWave; primary IC on Relay, the control plane behind the largest AMD-based GPU clouds; ~20 years as a builder-leader across AI infrastructure, financial services, data platforms, manufacturing, and SaaS; scales orgs (100+ engineers over a career) and still ships hard systems (Rust, OCaml, ReasonML, Haskell, JavaScript); Founder/CTO of three ventures.
- [ ] Summary does **not** contain $350M, $1.55B, Christian, Father, GPU counts, site names, or “I raised this.”
- [ ] TensorWave `work[0].highlights` still includes the existing IC / leadership / Event API lines **and** a new highlight that TensorWave raised **$350M Series B** at **$1.55B** post-money and is an AMD-powered / AMD-exclusive AI cloud (employer context).
- [ ] `projects[0]` is `crossr-skills` with url `https://scull7.github.io/crossr-skills/` and description equivalent to: Agent skills + harness for long-running coding agents (“forged in the Cross”). Secondary GitHub `https://github.com/scull7/crossr-skills` is stored on the project object (not hard-coded in a view).
- [ ] `projects[1]` is `ferro-wg` with url `https://github.com/scull7/ferro-wg` and description equivalent to: Rust WireGuard client with TUI and swappable backends (boringtun, neptun, gotatun).
- [ ] cents, bs-result, bs-sql-composer, bs-sql-common, bs-mysql2, and TensorWave Relay remain; no velumobex project.
- [ ] Home buffer shows the new summary paragraphs from JSON. `experience.md` / `highlights.md` show the new TensorWave highlight from JSON. `opensource.md` lists crossr-skills then ferro-wg with those one-liners and primary URLs, then the existing OSS set.
- [ ] Top-level `philosophy` is the authorized two-paragraph text (MES → HSM → GPU-cloud control planes; isolate effects / keep the core pure / measure the line; AVRIL plans, AXEL executes, nothing ships unblessed; crossr-skills is that discipline made public). It is **not** concatenated into `basics.summary`.
- [ ] `meta.lastModified` is a real ISO-8601 timestamp of this edit and is not `2026-07-12T00:00:00`.
- [ ] Education / certificates / `basics.label` are unchanged.
- [ ] `docs/ROADMAP.md` first content paragraph (or callout) points at the two `docs/plans/phase1-content-branding-avril-*.md` files and does not rewrite Phases 0.2–5.

## dependencies
none

## notes

