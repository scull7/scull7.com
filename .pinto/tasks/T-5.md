+++
id = "T-5"
title = "Fix broken or retired-domain URLs already on the site"
status = "todo"
rank = "m"
points = 2
labels = ["content", "seo", "phase1"]
depends_on = ["T-1"]
created = "2026-08-14T06:25:35.673666Z"
updated = "2026-08-14T06:49:44.435732Z"
+++

## why
Share targets and project links must resolve to living destinations, never the retired nathansculli.com domain.

## scope_in
- Audit URLs already represented on the site after T-1: `resume.json` `basics.url`, `basics.profiles[].url`, `work[].url`, `projects[].url`, home hero links, and `index.html` canonical/og url.
- Fix only URLs that (a) still contain `nathansculli.com`, or (b) fail in a normal browser (the intended page does not load). LinkedIn/crates.io bot-blocking HEAD 403 is **not** a break if a logged-out browser GET shows the profile/crate.
- Replacement destinations must already appear in this intent or in the pre-change `resume.json`. Empty string is allowed when no authorized replacement exists.

## scope_out
- Adding new social networks, calendar links, or unpublished project URLs.
- Restoring or redirecting nathansculli.com.
- GitHub/X **profile website** fields (human PBIs).
- Rewriting education or work copy.

## acceptance_criteria
- [ ] Repo search of tracked site files (`public/`, `src/`, `index.html`) finds **zero** `nathansculli.com` URL strings.
- [ ] Each remaining profile / project / work / hero URL either loads the intended public page in a browser or is intentionally empty **only** because it was already empty, was `nathansculli.com`, or was actually broken with no authorized replacement.
- [ ] Non-empty-stay-non-empty: every URL that was non-empty in the pre-T-5 loaded `resume.json` / hero / `index.html` stays non-empty unless it contained `nathansculli.com` or was actually broken (intended page does not load) with no authorized replacement. Emptying a working URL to pass the audit fails.
- [ ] Must-keep oracle (still present and non-empty after this PBI): `https://github.com/scull7`, `https://linkedin.com/in/scull7`, `https://crates.io/users/scull7`, `https://tensorwave.com`, T-1 primary project URLs `https://scull7.github.io/crossr-skills/` and `https://github.com/scull7/ferro-wg` (and T-1 stored GitHub secondary `https://github.com/scull7/crossr-skills`), and home hero hrefs `mailto:nathan@vegasbuckeye.com`, LinkedIn, GitHub.
- [ ] Live rendered DOM of home + `opensource.md` + `experience.md` contains **zero** `nathansculli.com` strings (hrefs or visible text).
- [ ] No new URL was introduced that is not in the intent table or the previous `resume.json`.
- [ ] `basics.url` and `og:url` remain `https://scull7.com` (with or without trailing slash, consistent with current tags).

## dependencies
T-1

## notes

