+++
id = "T-25"
title = "Archive MoonBit-era docs and leave one Melange stack pointer"
status = "done"
rank = "y"
points = 2
labels = ["phase3", "docs"]
created = "2026-08-22T05:50:51.049704Z"
updated = "2026-08-28T07:05:00.000000Z"
+++

## why
`docs/IMPROVEMENT-PLAN.md` and MoonBit-era guidelines/verifications still describe MoonBit/Luna as the live stack, so agents target the wrong compiler.

## scope_in
- Move or relabel `docs/IMPROVEMENT-PLAN.md` and MoonBit-era guidelines / `verification-item-*` so they are not live guidance.
- Leave **one** pointer from ROADMAP and/or README that Melange is the live backend.
- Do not change Elm/Melange product source.

## scope_out
- routing / print / T-8 / new npm except elm-test (where relevant)
- Rewriting shipped Phase 1/2 product behavior
- Changing `src/` Elm or Melange code
- Netlify dashboard opam cache
- Hash routing; print CSS; elm-test; GitHub Actions

## acceptance_criteria
- [x] `docs/IMPROVEMENT-PLAN.md` is either **moved** (e.g. `docs/archive/`) **or** the file at that path is clearly labeled archived/historical in the first screen of the file (not live MoonBit implementation guidance). An unchanged live MoonBit plan at `docs/IMPROVEMENT-PLAN.md` **fails**.
- [x] MoonBit-era guidelines and `verification-item-*` files are archived or labeled historical. Leaving them as current-stack instructions **fails**.
- [x] README.md and/or `docs/ROADMAP.md` contains a pointer that the live stack is **Melange** (OCaml/Reason toward JS), not MoonBit. Zero Melange pointers **fails**.
- [x] Those same entry docs do not present MoonBit as the current implementation target. "We are building in MoonBit" as present-tense live guidance **fails**.
- [x] No `src/` Elm/Melange behavior change is required — `src/` is untouched by this card (`git status --short src/` is empty), so the e2e condition does not arise. (The runner is now `npm run test:e2e`; the card predated the move off `e2e/*.mjs`.)

## dependencies
none

## notes

## notes
- 2026-08-28: 11 files moved with `git mv` (history preserved) to `docs/archive/`: `IMPROVEMENT-PLAN.md`, `guidelines-item-1..5-*.md`, `verification-item-1..5.md`. Each keeps its H1 and gains an ARCHIVED banner in the first screen naming the live stack; cross-references between them were repointed at the archive paths so nothing dangles.
- `docs/archive/README.md` indexes them, records that the intent was re-planned onto the Pinto board (T-20..T-25), and says plainly not to implement from anything in the directory.
- ROADMAP had a second, worse drift the card did not name: §2 still called Melange "local WIP / uncommitted" and told readers to "treat ReScript as production". Corrected to the shipped reality, since an agent reading it would have targeted the wrong compiler just as surely as the MoonBit docs. The three MoonBit to-do lines are struck through and point at the archive.
- `docs/plans/*` still mention MoonBit, deliberately: they are dated AVRIL planning records whose task descriptions *are* "archive the MoonBit docs". Rewriting them would falsify the record, and they already state the stack is "Not ReScript. Not MoonBit."
