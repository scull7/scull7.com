+++
id = "T-9"
title = "Add philosophy.md terminal buffer from JSON philosophy"
status = "done"
rank = "ji"
points = 2
labels = ["content", "ux", "phase1"]
depends_on = ["T-1"]
start_at = "2026-08-15T07:49:00.649073Z"
done_at = "2026-08-15T15:55:15.946744Z"
commits = ["0ef8c4b"]
created = "2026-08-14T06:32:55.651568Z"
updated = "2026-08-15T15:55:15.946744Z"
+++

## why
A visitor can read the short philosophy in its own vim buffer, and a later PDF can reuse the same JSON field — it is not merged into the home summary.

## scope_in
- Catalog entry for `philosophy.md` only (sidebar, `:ls`, `:e` / `:open`, command-palette `:e` row).
- Buffer body is exactly the JSON `philosophy` field (paragraphs preserved; no hard-coded career/philosophy facts in Elm).
- Same terminal chrome as cents (buffer tab + body + statusline).
- Help.txt and/or palette remain able to discover the new buffer (palette `:e` list is sufficient if `:ls` lists it).

## scope_out
- `crossr-skills.md` buffer (separate PBI).
- Merging philosophy into `basics.summary` or the home summary.
- New ferro-wg buffer.
- `:mail`, last-updated chrome, JSON-LD, OG, PDF export.
- Christian / Father copy on the site.
- Sermon-length rewrite. Marketing landing page or non-terminal chrome.
- Splitting `Main.elm`. New dependencies.

## acceptance_criteria
- [x] Sidebar buffer list includes `philosophy.md` (or the exact id chosen, visible as that label).
- [x] `:e philosophy.md` (and `:open` / palette equivalent) opens a buffer whose visible text is exactly the JSON `philosophy` field (paragraphs preserved).
- [x] Mutation fixture: in a local `resume.json`, change `philosophy` to a distinct dummy two-paragraph string, reload; the buffer visible text equals that new value character-for-character, paragraphs preserved (not the T-1 authorized copy).
- [x] Empty fixture: remove `philosophy` or set it to `""`; the buffer does not show substitute or fallback copy — empty body or no catalog row. Hard-coded authorized philosophy text in that case fails.
- [x] `:ls` lists `philosophy.md`. Command palette in `:` mode offers an `:e` row for it.
- [x] Opening the buffer uses the same terminal chrome as cents (buffer tab + body + statusline); no second non-terminal layout.
- [x] No philosophy text is duplicated into `basics.summary` or the home summary.
- [x] This PBI does not add a `crossr-skills.md` buffer.

## dependencies
T-1

## notes
- **Superseded 2026-08-21 (human):** philosophy is internal documentation. Do not expose a public buffer or a `philosophy` field on `/resume.json`.
- Unpublish: `675b2fb` in [PR #5](https://github.com/scull7/scull7.com/pull/5) (merged `e8a6030`). Live catalog has no `philosophy.md`; public JSON has no `philosophy` key.

## Execution Evidence
- GAN: Reviewer BLESS · Tester BLESS · Architect BLESS
- Commit: 0ef8c4b (original buffer)
- Unpublish: 675b2fb
- elm:make + e2e PASS at original ship
- Live fixtures at original ship: exact body match, mutation, empty/missing (row hidden), :ls/palette, chrome, no home leak
- T-2 crossr-skills catalog unchanged
- Post-unpublish: sidebar / `:e` / `/resume.json` do not expose philosophy
